# 포트폴리오 프로젝트 설명 기획

> `/portfolio-pdf` 문서에서 각 프로젝트를 어떻게 설명할지 정하는 작업 메모. 콘텐츠 원본은 `src/data/profileDoc.ts` (`PROFILE_PLATFORM.projects` / `PROFILE.projects`).

## 확정된 구조

프로젝트 하나당:

1. **개요** (`desc`) — 한 줄 요약
2. **담당 업무** (`career.groups`에서 `proj` 태그로 연결된 항목)
3. **Skills** (`stack`)
4. **디테일 스토리** (`blocks[]`, N개) — 각각:
   - `label`: 제목
   - `situation` + `cause`: AS-IS (상황 + 원인)
   - `actions[]`: TO-BE (조치)
   - `result`: 결과

## 원칙

- **분량을 억지로 맞추지 않는다.** 스토리 개수는 실제로 다룰 소재가 있는 만큼만. 옅은 이야기로 채우면 품질이 떨어짐.
- 스타일 참고: `temp/sample.md` (동료 kangsj12의 뤼튼 지원용 포트폴리오, 안영준 것 아님 — 톤/깊이 참고용)

## 현재 상태 (인프라 탭 기준)

| 프로젝트 | 기간 | 블록 수 | 비고 |
|---|---|---|---|
| NIPA 위성 변화탐지 플랫폼 | 2025.07~진행중 | 2 | 진행 중이라 정리된 스토리 적음 |
| KARI 위성영상 AI 처리 플랫폼 | 2023.10~2025.07 | 5 | 회사 모든 K8s 플랫폼의 출발점 - 소재 많음 |
| 국가보안기관 위성영상 AI 처리 플랫폼 | 2024.07~진행중 | 3 | 운영 + 신규 구축 두 단계 혼재 |

## 결정 사항 (진행하며 기록)

- [x] **개요(`desc`)는 STAR의 Situation+Task만 담당.** 해결책(Action)·결과(Result)는 언급하지 않는다 — 그건 담당 업무 + `blocks[]`(AS-IS/TO-BE = 마이크로 STAR)가 이미 하고 있음. 개요는 "왜 어려운 문제였는지"만 숫자·제약 중심으로 짧게.
- [x] **아키텍처 도면(`archImage`) 최종 원칙 — 상세페이지 것을 그대로 들고온다.** 소스는 `blocks[]`(개선 스토리)나 `desc`(개요)가 아니라 각 프로젝트 상세페이지(`src/components/retrospectives/*.tsx`)의 "아키텍처" 섹션 전체 — FlowNode 구조도 + 그 아래 딸린 호출 흐름/CI-CD 흐름 등 번호 시퀀스까지 전부 포함. 일부만 골라 그리거나 다른 소스(`desc`)에서 지어내면 안 됨 (KARI "CesiumJS 뷰어", NIPA "Envoy Gateway 생략" 등 반복 실수). 새 섹션이 나중에 추가되면 그것도 반영.
  - ~~박스 수 최소화, 도면은 가로로 넓고 얕게~~ → 상세페이지 내용을 다 담다 보면 자연히 세로로 길어질 수 있음. 폐기.
- [x] **페이지 상하좌우 여백은 항상 지킨다.** `.sheet-inner`의 padding을 벗어나는 음수 마진/풀블리드 금지 (전에 아키텍처 이미지를 페이지 끝까지 채우려다 실제 인쇄 시 여백 밖으로 넘칠 뻔함). 이미지를 키우고 싶으면 뷰박스/폰트 크기로 키우되 컨테이너 폭 안에서.
- [x] **페이지네이션 안정성**: JS로 요소를 페이지 경계 기준 절대좌표에 배치하는 로직(장식 등)은 페이지 수 계산과 얽혀 버그(63페이지 폭주)를 낼 수 있음 — 대신 고정 크기 컨테이너(`.w-cover`) 기준 상대좌표만 사용. 페이지 분할 보정은 단일 패스로 처리(재시작 방식 금지).

## 열린 질문

- 프로젝트 순서를 지금(NIPA → KARI → 국가보안기관) 그대로 둘지
- `situation`/`cause` 서술 톤·길이 기준
- 프로젝트별로 빼거나 새로 추가하고 싶은 스토리
